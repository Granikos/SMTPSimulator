(function () {
    angular.module('Recieve', ['ui.bootstrap.modal'])

        .controller('RecieveController', [
            '$scope', '$modal', '$q', function ($scope, $modal, $q) {
            }
        ]);
})();