(function () {
    angular.module('Send', ['ui.bootstrap.modal'])

        .controller('SendController', [
            '$scope', '$modal', '$q', function ($scope, $modal, $q) {
            }
        ]);
})();